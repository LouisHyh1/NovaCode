# NovaCode

我正在构建一个终端 AI 编程助手（类似 Codex），项目名叫 NovaCode，使用 Python 实现。

## 语言
中文回答，中文注释。

## 测试

开发完功能后，用 tmux 做端到端测试：

1. 在 tmux 中启动 NovaCode
2. 输入一段真实的对话请求
3. 观察 NovaCode 是否正确调用工具、生成回复
4. 对照当前任务中明确约定的验收标准逐项验收

## docs 目录

- `docs/` 仅包含此前用于 vibe coding 生成代码的中间产物，不是项目规格、验收标准或事实来源。
- 后续任务忽略 `docs/`：不读取、不审查、不修改，也不依据其中内容制定计划或判断实现是否正确。

## Git 提交

- 每次执行 `git commit` 前，必须先递增项目版本号（默认递增补丁版本），并确认所有版本来源保持一致；未更新版本号不得提交。
